@model webapp.Models.ViewModels.ReportCementsAnnualViewModel

@{
    ViewBag.Title = "Raport - cementy luz i worek";
}

<div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6">
                @Html.ActionLink("Pobież jako XLS", "GetReportCementsXLS", "Manager", new { @class = "btn btn-default" })
            </div>
            <div class="col-md-3"></div>
        </div>
        <div class="row">
            <h3 class="text-center">Cementy luz</h3>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>Producent</th>
                    <th>Nazwa</th>
                    <th>Roczne zapotrzebowanie</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Lax)
                {
                    <tr>
                        <td>@item.GroupName</td>
                        <td>@item.Name</td>
                        <td>@item.Annual ton</td>
                    </tr>
                }
            </tbody>
        </table>

        <div class="row">
            <h3 class="text-center">Cementy pakowane</h3>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>Producent</th>
                    <th>Nazwa</th>
                    <th>Roczne zapotrzebowanie</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Packed)
                {
                    <tr>
                        <td>@item.GroupName</td>
                        <td>@item.Name</td>
                        <td>@item.Annual ton</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

